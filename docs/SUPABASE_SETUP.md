# Подключение общей базы Supabase

CRM умеет работать в двух режимах:

- без переменных окружения — старый локальный режим `localStorage`;
- с Supabase — обязательный вход, общая база между устройствами, ревизии, серверные резервные копии и роли.

## 1. Создайте проект

Создайте обычный Supabase project. В SQL Editor выполните файл:

`supabase/001_crm_cloud.sql`, затем `supabase/002_role_security_admin_tools.sql`, затем `supabase/003_cash_shifts.sql`, затем `supabase/004_mechanic_accountant_roles.sql`, затем `supabase/005_order_media_storage.sql`, затем `supabase/006_security_hardening.sql`, затем `supabase/007_atomic_stock_reservation.sql`

## 2. Создайте пользователей

В Supabase откройте **Authentication → Users** и создайте нужных пользователей: владельца, партнёра, приёмщика, запчастиста, механиков и бухгалтера.

Затем в SQL Editor:

```sql
insert into public.crm_workshops(name)
values ('Сервис Игоря')
returning id;
```

Скопируйте UUID автосервиса и UUID пользователей из Authentication → Users:

```sql
insert into public.crm_members(workshop_id, user_id, role, display_name) values
('<WORKSHOP_UUID>', '<IGOR_USER_UUID>', 'owner', 'Игорь'),
('<WORKSHOP_UUID>', '<PARTNER_USER_UUID>', 'partner', 'Партнёр'),
('<WORKSHOP_UUID>', '<ADVISOR_USER_UUID>', 'advisor', 'Юра'),
('<WORKSHOP_UUID>', '<PARTS_USER_UUID>', 'parts', 'Запчастист'),
('<WORKSHOP_UUID>', '<MECH1_USER_UUID>', 'mechanic', 'Механик 1'),
('<WORKSHOP_UUID>', '<ACCOUNTANT_USER_UUID>', 'accountant', 'Елена');
```

Роли: `owner`, `partner`, `advisor`, `parts`, `mechanic`, `accountant`.

## 3. Добавьте переменные сборки

Project Settings → API:

- Project URL → `VITE_SUPABASE_URL`
- anon / publishable key → `VITE_SUPABASE_ANON_KEY`

Для локального запуска скопируйте `.env.example` в `.env.local`.

Для GitHub Pages добавьте эти две переменные в:
**Repository → Settings → Secrets and variables → Actions → Variables**.

Не используйте service_role key в браузере или GitHub Pages.

## 4. Первый перенос данных

После следующей сборки CRM покажет экран входа.

Войдите владельцем. Если сервер пустой, в **Настройки → Общая база** будет кнопка
**«Перенести текущую базу на сервер»**. Она отправит текущие данные браузера на сервер один раз.

После этого другие устройства после входа загрузят ту же базу автоматически.

## Как работает защита от одновременной работы

У каждой серверной версии есть номер ревизии. CRM сохраняет только поверх той ревизии,
которую пользователь реально загрузил. Если два устройства изменили базу одновременно,
клиент получает свежую версию, переносит свои локальные изменения поверх неё и повторяет
сохранение. Разные сущности (например, разные заказ-наряды) поэтому не затирают друг друга.

## Резервные копии

Перед изменением сервер автоматически сохраняет полную копию базы, если за последние 24 часа
копии ещё не было. Владелец/партнёр также могут сделать ручную копию из настроек.

## Важно про роли

На этом этапе сервер строго изолирует данные одного автосервиса от другого и проверяет членство.
Интерфейс ограничивает разделы по роли. Для абсолютной защиты отдельных финансовых полей от
технически подготовленного сотрудника следующим шагом данные надо разнести из общего JSON-снимка
по отдельным таблицам/операциям с RLS на уровне строк и полей. Не выдавайте доступ к Supabase
Dashboard сотрудникам.


## Обновление уже созданного проекта

Если первая схема уже была выполнена раньше, повторять её не нужно. Выполните только:

`supabase/002_role_security_admin_tools.sql`

После этого:
- приёмщик не получает расходы, счета и зарплатные значения сотрудников;
- запчастист не получает платежи, счета, скидки и цены работ;
- приёмщик не может произвольно редактировать складские справочники; выдача заказа при этом синхронизирует фактическое списание склада;
- владелец и партнёр видят список резервных копий и журнал действий в Настройках;
- резервную копию можно восстановить из интерфейса CRM.


## Кассовые смены

После обновления CRM выполните:

`supabase/003_cash_shifts.sql`

Эта миграция скрывает кассовые смены от приёмщика и запчастиста на сервере. Управление кассой остаётся только у владельца и партнёра через раздел **Финансы → Касса**.


## Механики и бухгалтер

После выполнения `supabase/004_mechanic_accountant_roles.sql`:

- **mechanic** получает только заказ-наряды, где хотя бы одна работа назначена на его `display_name`;
- цены работ, оплаты, финансы, склад и зарплаты механику сервер не отдаёт;
- механик может менять только `workStatus` и `workSessions` своих работ;
- **accountant** открывает Финансы, Отчёты, Сотрудники и печатные документы, но не управляет ремонтом и складом.

Для механика `display_name` в `crm_members` должен **точно совпадать** с именем исполнителя в CRM. Например: `Механик 1`.


## Фото и видео заказов

После выполнения `supabase/005_order_media_storage.sql` создаётся приватный bucket `order-media`.

- приёмщик, владелец и партнёр могут добавлять фото/видео по заказам;
- механик может добавлять и смотреть медиа только по заказам, где на его `display_name` назначена работа;
- файлы не кладутся внутрь общего JSON-снимка: в заказе хранится только метадата и путь;
- максимальный размер одного файла — 25 МБ;
- без Supabase локально можно сохранять только сжатые фото; видео требует серверное хранилище.


## Усиление безопасности и атомарный резерв склада

После `005_order_media_storage.sql` обязательно выполните:

1. `supabase/006_security_hardening.sql`
2. `supabase/007_atomic_stock_reservation.sql`

Миграция `006` усиливает серверные ограничения ролей и доступ к фото/видео заказов.

Миграция `007` добавляет серверную операцию резерва запчасти. Она блокирует строку общей базы на время проверки остатка, поэтому два пользователя не смогут одновременно зарезервировать одну последнюю единицу. В облачном режиме резерв требует связи с сервером; без интернета CRM не подтверждает критичную складскую операцию, чтобы не создать скрытый двойной резерв.


## Атомарные оплаты клиентов

После `007_atomic_stock_reservation.sql` выполните:

`supabase/008_atomic_order_payments.sql`

Миграция `008` проводит оплаты и возвраты под серверной блокировкой общей базы. Если два устройства одновременно пытаются закрыть один остаток долга, сервер принимает только допустимую сумму и возвращает второму устройству свежие данные. Наличные дополнительно проверяются по открытой кассовой смене.


## Атомарные кассовые смены

После `008_atomic_order_payments.sql` выполните:

`supabase/009_atomic_cash_shifts.sql`

Миграция `009` не позволяет двум устройствам одновременно открыть разные кассовые смены. При закрытии сервер заново считает ожидаемый остаток по наличным оплатам, возвратам клиентам, расходам и возвратам поставщиков. Если есть расхождение, закрытие без комментария отклоняется.


## Синхронизация списания склада приёмщиком

После `009_atomic_cash_shifts.sql` выполните:

`supabase/010_advisor_stock_issue_sync.sql`

Миграция `010` обновляет `crm_save_state`: приёмщик при выдаче автомобиля сохраняет фактический остаток `stock` вместе с заказом и движением склада. Без этой миграции списание могло остаться только на устройстве, с которого выдали автомобиль.


## Атомарная приёмка и возврат поставщику

После `010_advisor_stock_issue_sync.sql` выполните:

`supabase/011_atomic_stock_receiving_returns.sql`

Миграция `011` проводит приёмку и возврат поставщику под блокировкой общей базы. Сервер повторно проверяет актуальный остаток, резервы, занятую ячейку и открытую кассовую смену для наличной закупки. Параллельные операции с двух устройств больше не могут перетереть количество одной позиции.


## Проверка версии серверной схемы

После `011_atomic_stock_receiving_returns.sql` выполните:

`supabase/012_server_capabilities.sql`

После этого **Настройки → Готовность к реальной работе** показывает версию серверных миграций. Если функция отсутствует, CRM явно сообщает, что серверную схему нужно обновить, вместо того чтобы обнаруживать проблему только при складской или кассовой операции.


## Атомарное создание заказ-наряда

После `012_server_capabilities.sql` выполните:

`supabase/013_atomic_order_create.sql`

Миграция `013` создаёт нового клиента, автомобиль и заказ-наряд внутри одной серверной транзакции. Сервер повторно проверяет дубли телефона, госномера/VIN и занятость подъёмника, а затем выдаёт уникальные последовательные коды клиента, автомобиля и номер заказ-наряда. Это исключает дубли при одновременной работе Игоря, Юры и приёмщика с разных устройств.


## Атомарное управление автомобилями

После `013_atomic_order_create.sql` выполните:

`supabase/014_atomic_vehicle_management.sql`

Миграция `014` защищает отдельное добавление и редактирование автомобилей в карточке клиента от дублей госномера/VIN при работе с нескольких устройств. Удаление также выполняется под серверной блокировкой: если на другом устройстве уже успели создать заказ по автомобилю, удалить его нельзя.


## Идемпотентные денежные операции

После `014_atomic_vehicle_management.sql` выполните:

`supabase/015_idempotent_financial_mutations.sql`

Миграция `015` защищает ручные расходы, выплаты зарплаты и подтверждение возврата от поставщика. Каждая операция получает стабильный идентификатор: если ответ сервера потерялся и клиент повторил запрос, сервер возвращает уже сохранённое состояние вместо второго списания/начисления. Одновременно повторно проверяются кассовая смена, остаток сдельной зарплаты и текущее состояние возврата поставщика.


## Идемпотентная смена статуса и выдача

После `015_idempotent_financial_mutations.sql` выполните:

`supabase/016_idempotent_order_status.sql`

Миграция `016` переносит смену статуса заказ-наряда на сервер. Переход, завершение работ, фиксация процента зарплаты и списание/возврат запчастей выполняются одной транзакцией. Если ответ потерялся и запрос повторился, уже применённый статус возвращается как успешный no-op — второе движение склада не создаётся. Перед выдачей сервер повторно проверяет завершённость работ, переплату, заказанные запчасти и актуальный остаток.
